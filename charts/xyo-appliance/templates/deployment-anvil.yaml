{{- if .Values.anvil.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "xyo-appliance.fullname" . }}-anvil
  labels:
    {{- include "xyo-appliance.componentLabels" (dict "root" . "component" "anvil") | nindent 4 }}
spec:
  replicas: {{ .Values.anvil.replicaCount }}
  selector:
    matchLabels:
      {{- include "xyo-appliance.componentSelectorLabels" (dict "root" . "component" "anvil") | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "xyo-appliance.componentLabels" (dict "root" . "component" "anvil") | nindent 8 }}
    spec:
      {{- include "xyo-appliance.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "xyo-appliance.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: anvil
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ include "xyo-appliance.imageRegistry" (dict "image" .Values.anvil.image "root" .) }}{{ .Values.anvil.image.repository }}:{{ .Values.anvil.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.anvil.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.anvil.service.targetPort | default 9093 }}
              protocol: TCP
          env:
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "xyo-appliance.fullname" . }}-config
                  key: LOG_LEVEL_ANVIL
          {{- if .Values.anvil.probes.liveness.enabled }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.anvil.probes.liveness.tcpSocket.port }}
            initialDelaySeconds: {{ .Values.anvil.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.anvil.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.anvil.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.anvil.probes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.anvil.probes.readiness.enabled }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.anvil.probes.readiness.tcpSocket.port }}
            initialDelaySeconds: {{ .Values.anvil.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.anvil.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.anvil.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.anvil.probes.readiness.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.anvil.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.anvil.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.anvil.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.anvil.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
