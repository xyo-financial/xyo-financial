{{- if .Values.lexicon.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "xyo-appliance.fullname" . }}-lexicon
  labels:
    {{- include "xyo-appliance.componentLabels" (dict "root" . "component" "lexicon") | nindent 4 }}
spec:
  replicas: {{ .Values.lexicon.replicaCount }}
  selector:
    matchLabels:
      {{- include "xyo-appliance.componentSelectorLabels" (dict "root" . "component" "lexicon") | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "xyo-appliance.componentLabels" (dict "root" . "component" "lexicon") | nindent 8 }}
    spec:
      {{- include "xyo-appliance.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "xyo-appliance.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: lexicon
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ include "xyo-appliance.imageRegistry" (dict "image" .Values.lexicon.image "root" .) }}{{ .Values.lexicon.image.repository }}:{{ .Values.lexicon.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.lexicon.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.lexicon.service.targetPort | default 9092 }}
              protocol: TCP
          env:
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "xyo-appliance.fullname" . }}-config
                  key: LOG_LEVEL_LEXICON
          {{- if .Values.lexicon.probes.liveness.enabled }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.lexicon.probes.liveness.tcpSocket.port }}
            initialDelaySeconds: {{ .Values.lexicon.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.lexicon.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.lexicon.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.lexicon.probes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.lexicon.probes.readiness.enabled }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.lexicon.probes.readiness.tcpSocket.port }}
            initialDelaySeconds: {{ .Values.lexicon.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.lexicon.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.lexicon.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.lexicon.probes.readiness.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.lexicon.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.lexicon.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.lexicon.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.lexicon.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
